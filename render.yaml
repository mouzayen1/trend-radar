services:
  - type: worker
    name: trend-radar
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -u main.py
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: DISCORD_WEBHOOK_URL
        sync: false
      - key: YOUTUBE_API_KEY
        sync: false
